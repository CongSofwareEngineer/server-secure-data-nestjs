export enum DB_COLLECTION {
  User = 'User',
  Account = 'Account',
  Finance = 'Finance',
}

export const DB_NAME = 'secure-data'

export enum MATH_DB {
  $all = '$all',
  $in = '$in',
  $gt = '$gt',
  // >=
  $gte = '$gte',
  // <=
  $lte = '$lte',
  $lt = '$lt',
  $nor = '$nor',
  $and = '$and',
  $or = '$or',
  $where = '$where',
  $MergeObjects = '$mergeObjects',
  $regex = '$regex',
  $elemMatch = '$elemMatch',
}

